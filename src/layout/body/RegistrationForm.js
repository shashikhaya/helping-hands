const RegistrationForm = ({title,text,button}) => {
    return (
        <div>
            <h5>{title}</h5>
            <p>{text}</p>
            <button>{button}</button>
        </div>
    )
}

export default RegistrationForm
